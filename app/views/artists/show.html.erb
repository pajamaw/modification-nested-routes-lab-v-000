<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li>
  <% end %>
</ul>
<%= link_to 'Edit Artist', edit_artist_path(@artist)%>
<br>
<%= link_to 'Back to All Artists', artists_path%>
